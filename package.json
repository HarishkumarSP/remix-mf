{
	"name": "remix-mf",
	"version": "0.0.1",
	"scripts": {
		"dev:host": "pnpm --filter host-app run dev",
		"dev:remote": "pnpm --filter remote-app run dev",
		"dev:all": "pnpm -r run dev",
		"build:host": "pnpm --filter host-app run build",
		"build:remote": "pnpm --filter remote-app run build",
		"build:all": "pnpm -r run build",
		"start:host": "pnpm --filter host-app run start",
		"start:remote": "pnpm --filter remote-app run start",
		"start:all": "pnpm -r run start"
	}
}
